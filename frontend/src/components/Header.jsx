import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '1rem', background: '#333', color: '#fff', textAlign: 'center' }}>
            <h1>MERN Stack Prototype</h1>
        </header>
    );
};

export default Header;
